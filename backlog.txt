Project Backlog, Improvement Ideas and General Remarks

Immediate next steps:
- Refactor the discord user / coc player linking for admins with a nice web-ui.
- Add a feature to DM users of the guild server to remind them to link their coc player accounts. the dm could contain a button that easily allows them to do the linking. The mail could be signed by a configurable server admin so that it is more convincing.
- Handling of DMs that can't be sent: e.g. notify the admin
- Block players from CWL participation (just show an icon not a real technical blocking!) e.g. when players miss attack and are punished for that
- Known lower-priority follow-up (not fixed,
  admin-only/rarely invoked, no crash risk): the `/admin` CWL-group-backfill
  handler in qapbot/QBdiscocmdshelper_admin_command.py
  (`_handle_backfill_cwl_groups_inner`) filters war_summary by `cwl_season` alone
  without a supporting index (existing index is `(clan_tag, cwl_season)`),
  which likely full-scans hot+history war_summary; low impact since it's a
  manually-triggered maintenance command, not user-facing.
- Add to /whois player: clan history tab
- Monitor [PRE-SCAN-TIMING] archive_scan cost in PHASE-2 of update cycle through next CWL season (archive/
  can grow ~4x current ~320K files during CWL) — revisit incremental
  archive-filename index if it grows significantly above the current ~1s
  baseline. Full risk analysis + deferred implementation plan in
  qapbot/docs/ARCHIVE_SCAN_PERFORMANCE_ANALYSIS.md
- Check [PHASE-3B-SCAN-TIMING] (tier3_stat_calls, elapsed) during the next CWL
  season. PHASE 3B's ~26K-clan candidate scan is expected to hit Tier 3
  (real os.path.getmtime() syscalls) much more often during CWL, since more
  clans have active/changing war states than outside CWL season — can't
  meaningfully assess impact right now since we're not currently in a CWL
  season. Yield points (every 2000 clans) already added 2026-07-18; revisit
  whether Tier 2's in-memory metadata coverage needs extending if
  tier3_stat_calls/elapsed turn out high.
- Wrap the war simulation in a small piece of C code to gain performance
- Add /admin Backup Data command that mimics the ArchiveOld.bat behaviour and auto-moves excessive .\archive file numbers to .\archive_old with nightly maintenance auto-run
- log_time_gaps.py script anwenden und weitere blocking und langläufer operationen finden
- Should probably be family: Posting leaderboard for clan #5053A40B67
- Use custom emojis everywhere in the code
- Update all channel messages when guild-language is switched
- Create a Newsfeed-Channel (or Updates-Channel) that gets updates on what is going on
- Player activity monitor, logins, raid weekends, clangames
- Add subscription-type for playerlist for clan / clan-family and add function to update channels when player-lists change
- /clan management: Regular users should be able to use the viewing functions but not the action buttons
- New Feature: Add scores for "th-level difference" and "up-hits/drops" in CW/CWL

### A6. Global mutable state sprawl in QBcore — [verified, low (works today, risk grows)]
15+ module-level flags/events (`maintenance_mode`, `db_maintenance_mode`, `maintenance_pending`,
`optimize_db_pending`, `cycle_idle_event`, `force_cycle_event`, ...). Each is individually fine;
collectively the implicit state machine has no single owner and every new flag multiplies
interactions. **Fix (opportunistic)**: group into one dataclass (`BotState`) next time a new flag
would be added — not worth a big-bang refactor on its own.

### B4. `compute_roster_stats_sync` — 3 sequential aggregate queries — [verified, low]
QBhelperfunctions.py ~3641-3723: three separate full aggregate queries over hot+history per
invocation. Each is index-supported and correct (post-materialize-fix); merging them is possible
but the join/GROUP BY shapes differ. **Fix**: only worth touching when the CWL Phase-3 extension
(attack counts) lands anyway — fold the count into an existing query instead of adding a 4th.

### B7. `save_user`/bulk user writes use per-row execute — [verified, low]
db_manager.py ~4944+: per-player `INSERT` in a loop inside the transaction. Correct and atomic;
at current maindata scale (a few hundred players total) not a bottleneck. **Fix**: switch to
`executemany()` opportunistically next time the function is edited — don't ship a standalone
change for this.

### D5. Oversized functions (structural debt, refactor only with a reason) — [verified]
The worst offenders, for when a feature touches them anyway:
- `main()` QapBot.py ~579-1640 (~1060 lines, Phases 1-3 inline)
- `admin()` QBdiscordcmds.py ~1176+ (~1200 lines incl. inline modal classes)
- `periodic_main()` QapBot.py ~1883-2380 (~500 lines)
- `generate_cwlinfo_embeds()` QBhelperfunctions.py ~1786-2264 (~478 lines)
- `save_war_object()` cache_manager.py ~2255-2633 (~378 lines)
Rule of thumb per project convention: don't refactor these standalone (huge diff, high regression
risk, 1517-test suite has good but not perfect coverage of their branches); extract the piece you
need when a change lands inside one.
