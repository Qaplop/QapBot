# Copy to .env.local (gitignored). Not secret — the OAuth Client ID is public by design — but
# it's environment-specific (DEV vs PROD Discord Application), so it isn't checked in.
VITE_CLIENT_ID=your-discord-application-client-id
